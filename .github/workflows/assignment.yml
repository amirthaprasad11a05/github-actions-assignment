name: Caching Assignment

on: push

jobs:
  cache:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: prime-cache-27f7809
      uses: actions/cache@v4
      with:
        key: cache-27f7809
        path: ~/.demo-cache/
    
    - name: Cache info
      run: echo "Cache setup complete"
      
    - name: Setup demo
      run: mkdir -p ~/.demo-cache && echo "OK" > ~/.demo-cache/test.txt
